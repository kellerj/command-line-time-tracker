/* istanbul ignore file */
const DATE_FORMAT = 'YYYY-MM-DD';
const DEFAULT_MINUTES = 60;
const TIME_FORMAT = 'h:mm a';
const LIST_DISPLAY_SIZE = 15;

module.exports = {
  DATE_FORMAT,
  DEFAULT_MINUTES,
  TIME_FORMAT,
  LIST_DISPLAY_SIZE,
};
